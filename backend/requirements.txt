# FastAPI and server
fastapi>=0.115.0  # Updated for Pydantic 2.6+ compatibility
uvicorn[standard]>=0.24.0
python-multipart==0.0.6

# Database
sqlalchemy>=2.0.36,<3.0.0  # Updated for Python 3.13 compatibility (2.0.36+ fixes typing issues)
# psycopg (newer, better Python 3.13 support) or psycopg2-binary (if wheels available)
psycopg[binary]>=3.1.0
# Alternative if psycopg doesn't work: psycopg2-binary>=2.9.10
alembic==1.12.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-decouple==3.8

# Pydantic (compatible with FastAPI 0.104+ and Python 3.13)
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.1.0
email-validator>=2.0.0  # Required for Pydantic email validation

# Redis (for caching)
redis==5.0.1

# Background tasks (optional - for ML workers)
celery==5.3.4
flower==2.0.1

# Date/Time utilities
python-dateutil==2.8.2

# Environment variables
python-dotenv==1.0.0

# HTTP client (for external APIs if needed)
httpx==0.25.1

# Data processing (using versions compatible with Python 3.13)
# NumPy 2.x for Python 3.13 compatibility (has pre-built wheels)
numpy>=2.0.0,<2.2.0
# Pandas 2.2.x works with NumPy 2.x
pandas>=2.2.0,<2.3.0

# PDF generation (for invoices)
reportlab==4.0.7
weasyprint==60.1

# Excel export
openpyxl==3.1.2

# Logging
loguru==0.7.2
